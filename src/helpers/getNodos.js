import "../data/Silobolsas_San_Genaro";

export const getNodos = () => {
    siloBolsas.find(campaña => campaña.name === name);
    // for (let i = 0; i < siloBolsas.length; i++) {
        // if (siloBolsas[i].name === name) {
        //   return siloBolsas[i];
        // }
    //   }
    //   return null; 
    }
  